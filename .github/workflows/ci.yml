name: CI

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths-ignore:
      - "*.md"
  pull_request:
    types:
      - opened
      - synchronize
      - ready_for_review
    paths-ignore:
      - "*.md"
  schedule:
    - cron: 0 16 * * *

permissions: read-all

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  cargo-doc:
    runs-on: ubuntu-latest
    env:
      RUSTFLAGS: "-Dwarnings"
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        if: ${{github.event_name=='pull_request'}}
      - name: Set up pixi
        uses: prefix-dev/setup-pixi@v0.9.1
        with:
          run-install: false
      - name: cargo-doc
        run: ./scripts/cargo-doc.ps1
        shell: pwsh

  test-build-win:
    strategy:
      fail-fast: false
      matrix:
        include:
          - machine: windows-latest
            os: win-64
    env:
      dnc_version: 17_5
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - name: restore dnc
        uses: actions/cache@v4
        with:
          path: ./temp/dnc${{env.dnc_version}}.exe
          key: dnc-${{ runner.os }}-${{env.dnc_version}}
      - run: ./scripts/install-dnc.ps1 -version ${{env.dnc_version}}

      - name: Set up pixi
        uses: prefix-dev/setup-pixi@v0.9.1
        with:
          run-install: false

      - uses: dtolnay/rust-toolchain@nightly

      - name: cargo-test
        run: ./scripts/cargo-test.ps1 --workspace --all-features --exclude fornax-py
        shell: pwsh

      - name: rattler-build
        run: ./crates/fornax-py/rattler/ci-fornax-py.ps1
        shell: pwsh

      - name: upload-assets
        uses: actions/upload-artifact@v4
        with:
          name: pyxis-python-${{runner.os}}
          path: |
            ./crates/fornax-py/rattler/output/win-64/*.conda
          if-no-files-found: error

      - name: Upload results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/codecov-action@v5

      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@v1
        with:
          fail_ci_if_error: true

  test-build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up pixi
        uses: prefix-dev/setup-pixi@v0.9.1
        with:
          run-install: false

      - uses: dtolnay/rust-toolchain@nightly

      - name: cargo-test
        run: ./scripts/cargo-test.ps1 --workspace --all-features --exclude fornax-py dnc
        shell: pwsh

      - name: rattler-build
        run: ./crates/fornax-py/rattler/ci-fornax-py.ps1
        shell: pwsh

      - name: upload-assets
        uses: actions/upload-artifact@v4
        with:
          name: pyxis-python-${{runner.os}}
          path: |
            ./crates/fornax-py/rattler/output/linux-64/*.conda
          if-no-files-found: error

      - name: Upload results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/codecov-action@v5

      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@v1
        with:
          fail_ci_if_error: true
